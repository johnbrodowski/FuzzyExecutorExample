# FuzzyExecutorExample
A stand-alone demonstration of a **fuzzy natural language executor** for UI automation.   This example parses commands like `"Calculator button 5 click"` and executes actions on the best-matching windows and elements.
